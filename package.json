{
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.3.2"
  },
  "scripts": {
    "prepare": "husky",
    "typecheck": "cd client && npm run type-check && cd ../server && npm run build"
  }
}
