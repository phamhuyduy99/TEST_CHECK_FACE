{
  "name": "mini-check-face-time",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "npm install && cd server && npm install && cd ../client && npm install && cd ../python-liveness-server && python -m venv venv && venv\\Scripts\\pip install -r requirements.txt",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "dev:python": "cd python-liveness-server && venv\\Scripts\\python app.py",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\" \"npm run dev:python\"",
    "dev:no-python": "concurrently \"npm run dev:server\" \"npm run dev:client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
